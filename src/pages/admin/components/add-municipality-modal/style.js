import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles(theme => ({
  palikaTypeSelectContainer: {
    [theme.breakpoints.up('md')]: {
      marginTop: theme.spacing(1)
    }
  }
}));
export default styles;